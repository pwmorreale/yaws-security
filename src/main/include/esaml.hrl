
%% XXX This is broken, needs to be platform independent.
%% Paths to the IDP and our own (SP) metadatas.
-define (IDP_XML, "/etc/yaws/idp_metadata.xml").
-define (SP_XML, "/etc/yaws/sp_metadata.xml").

